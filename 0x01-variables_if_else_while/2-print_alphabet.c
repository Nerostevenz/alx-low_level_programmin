#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - main block
 * Description: Print alphabet in lower case
 * Return: 0
 */
int main(void)
{
	for (ch = "a"; ch <= "z"; ch++)
		putchar(ch);
		putchar ("\n");
		getch();

	return (0);
}
