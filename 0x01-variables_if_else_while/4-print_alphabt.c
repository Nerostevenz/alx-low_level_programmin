#include <stdio.h>
#include <stdlib.h>
/**
 * main - main block
 * Description: print some words
 * Return: 0
 */
int main(void)
{
	char b = 'a';

	while (b <= 'z')
	{
		if (b != 'q' && b != 'e')
		putchar(b);
		c++;
	}
	putchar('\n');

	return (0);
}
