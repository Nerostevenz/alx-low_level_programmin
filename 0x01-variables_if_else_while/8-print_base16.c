#include <stdio.h>
/**
 * main - main block
 * Description: Print hexadecimals
 * Return: 0
 */
int main(void)
{
	char x;
	int i;

	for (i = 0; i < 10; i++)
	{
		putchar(i + '0');
	}
	for (x = 'a'; c < 'g'; x++)
	{
		putchar(x);
	}
	putchar('\n')

	return (0)
}
